cmake_minimum_required(VERSION 3.14)
project(
  blockchain
  VERSION 1.0
  DESCRIPTION "blockchain Assignment")
include_directories(include)
set(CMAKE_CXX_STANDARD 14)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
# set(LIBRARY_PATHS lib) find_library( CHECK_LIBRARY NAMES "libcheck-aarch64.so"
# PATHS ${LIBRARY_PATHS})

file(GLOB SOURCES "src/*.c")

add_link_options(-lcrypto -lrt)
add_executable(blockchain ${SOURCES})
# target_compile_options(blockchain PUBLIC -g -fsanitize=address)
# find_package(OpenSSL REQUIRED) target_link_libraries(blockchain OpenSSL::SSL
# OpenSSL::Crypto)
target_link_libraries(blockchain ~/a12-yinkak/lib/libcheck-aarch64.so)
add_compile_options(-g)
add_link_options(-g)
